export const metadata = {
  title: "Bracket Validation – BBMI Model Accuracy",
  description:
    "Internal validation of pre-tournament bracket predictions vs actual NCAA tournament results.",
  robots: "noindex, nofollow",
};

export default function BracketValidationLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
